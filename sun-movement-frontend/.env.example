# Email Configuration
# Choose one of the following email services:

# 1. Gmail SMTP (Recommended for development)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com

# 2. SendGrid (Recommended for production)
# SENDGRID_API_KEY=your-sendgrid-api-key
# SENDGRID_FROM=contact@sunmovement.vn

# 3. Resend (Alternative for production)
# RESEND_API_KEY=your-resend-api-key
# RESEND_FROM=contact@sunmovement.vn

# Email settings
EMAIL_TO=contact@sunmovement.vn
EMAIL_SERVICE=smtp
# Options: smtp, sendgrid, resend

# Next.js
NEXT_PUBLIC_SITE_URL=http://localhost:3000
